name: Generate Snake Game

on:
  schedule:
    # 매일 자정에 스네이크 게임을 생성합니다.
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v2

      - name: Generate Snake Game from GitHub Contribution Grid
        uses: Platane/snk@main
        with:
          github_user_name: leedonguk0809  # 본인의 GitHub 사용자 이름
          
      - name: Push generated Snake Game to the repository
        uses: EndBug/add-and-commit@v7
        with:
          author_name: github-actions[bot]
          author_email: github-actions[bot]@users.noreply.github.com
          message: "Generated Snake Game"

      - name: Update README with Snake Game
        run: |
          sed -i '/^<!-- snake -->/d' README.md
          echo "![GitHub Snake](https://github.com/leedonguk0809/leedonguk0809/raw/output/github-contribution-grid-snake.svg)" >> README.md
